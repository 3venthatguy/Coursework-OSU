;-------------------------------------------------------------------------------
; ECE 2560 Final Exam Part 1
;
; Name: Evan Menges.24
;-------------------------------------------------------------------------------
            .cdecls C,LIST,"msp430.h"       ; Include device header file

;-------------------------------------------------------------------------------
            .def    RESET                   ; Export program entry-point to
                                            ; make it known to linker.
;-------------------------------------------------------------------------------
            .text                           ; Assemble into program memory.
            .retain                         ; Override ELF conditional linking
                                            ; and retain current section.
            .retainrefs                     ; And retain any sections that have
                                            ; references to current section.
;-------------------------------------------------------------------------------
RESET       mov.w   #__STACK_END,SP         ; Isnitialize stackpointer
StopWDT     mov.w   #WDTPW|WDTHOLD,&WDTCTL  ; Stop watchdog timer
;-------------------------------------------------------------------------------
; Main loop here
;-------------------------------------------------------------------------------
            ; Disable the GPIO power-on default high-impedance mode
            bic.w   #LOCKLPM5, &PM5CTL0

            ; Configure red LED on P1.0
            bis.b   #BIT0, &P1DIR
            bis.b   #BIT0, &P1OUT     			; Start ON (set bit = LED on)

            ; Configure green LED on P9.7
            bis.b   #BIT7, &P9DIR
            bic.b   #BIT7, &P9OUT       		; Start OFF (clear bit = LED off)

            ; Initialize state variable
            mov.b   #0, R4                  	; R4 holds state (0-7)

; Configure Timer B0 to raise interrupts
            bis.w    #TBCLR, &TB0CTL
            bis.w    #TBSSEL__ACLK, &TB0CTL   	; connected to ACLK
            bis.w    #MC__CONTINUOUS, &TB0CTL  	; continuous mode
            bis.w    #CNTL__12, &TB0CTL       	; counter length = 12 bits
            bis.w    #ID__4, &TB0CTL        	; input divider
            bis.w    #TBIE, &TB0CTL        		; enable interrupts

            ; Enter Low Power Mode 3 with interrupts enabled
            nop                          		; Required before setting GIE
            bis.w   #GIE|LPM3, SR
            nop                                 ; CPU resumes here after interrupt
;-------------------------------------------------------------------------------
; Interrupt Service Routine
;-------------------------------------------------------------------------------
Timer_B0_ISR:
            bic.w   #TBIFG, &TB0CTL              ; Clear interrupt flag

            ; State machine with 8 states:
            cmp.b   #0, R4
            jeq     state_0						; 0: Red ON (1st)
            cmp.b   #1, R4
            jeq     state_0						; 1: OFF
            cmp.b   #2, R4
            jeq     state_0						; 2: Red ON (2nd)
            cmp.b   #3, R4
            jeq     state_3						; 3: OFF
            cmp.b   #4, R4
            jeq     state_4						; 4: Green ON (1st)
            cmp.b   #5, R4
            jeq     state_5						; 5: OFF
            cmp.b   #6, R4
            jeq     state_6						; 6: Green ON (2nd)
            cmp.b   #7, R4
            jeq     state_7						; 7: OFF

            mov.b   #0, R4
            reti

state_0:    ; First red ON
            bis.b   #BIT0, &P1OUT                ; Red ON
            bic.b   #BIT7, &P9OUT                ; Green OFF
            inc.b   R4
            reti

state_1:    ; OFF after first red
            bic.b   #BIT0, &P1OUT                ; Red OFF
            bic.b   #BIT7, &P9OUT                ; Green OFF
            inc.b   R4
            reti

state_2:    ; Second red ON
            bis.b   #BIT0, &P1OUT                ; Red ON
            bic.b   #BIT7, &P9OUT                ; Green OFF
            inc.b   R4
            reti

state_3:    ; OFF after second red
            bic.b   #BIT0, &P1OUT                ; Red OFF
            bic.b   #BIT7, &P9OUT                ; Green OFF
            inc.b   R4
            reti

state_4:    ; First green ON
            bic.b   #BIT0, &P1OUT                ; Red OFF
            bis.b   #BIT7, &P9OUT                ; Green ON
            inc.b   R4
            reti

state_5:    ; OFF after first green
            bic.b   #BIT0, &P1OUT                ; Red OFF
            bic.b   #BIT7, &P9OUT                ; Green OFF
            inc.b   R4
            reti

state_6:    ; Second green ON
            bic.b   #BIT0, &P1OUT                ; Red OFF
            bis.b   #BIT7, &P9OUT                ; Green ON
            inc.b   R4
            reti

state_7:    ; OFF after second green
            bic.b   #BIT0, &P1OUT                ; Red OFF
            bic.b   #BIT7, &P9OUT                ; Green OFF
            mov.b   #0, R4                       ; Reset to state 0
            reti

;-------------------------------------------------------------------------------
; Stack Pointer definition
;-------------------------------------------------------------------------------
            .global __STACK_END
            .sect   .stack
;-------------------------------------------------------------------------------
; Interrupt Vectors
;-------------------------------------------------------------------------------
            .sect 	".int50"
            .short 	Timer_B0_ISR

            .sect   ".reset"                ; MSP430 RESET Vector
            .short  RESET
